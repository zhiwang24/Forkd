{
  "name": "sprint3-prototype-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.0.1",
    "firebase-functions": "^4.0.0"
  },
  "scripts": {
    "lint": "eslint . || true",
    "deploy": "firebase deploy --only functions"
  }
}
